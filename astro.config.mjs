import { defineConfig } from 'astro/config';

export default defineConfig({
  // Static output — no backend needed
  output: 'static',
  // Change this to your deployed domain when ready
  // site: 'https://salifehub.co.za',
});
